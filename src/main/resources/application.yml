server:
  port: 8080

spring:
  application:
    name: rsnotice
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: # ? ?
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cache:
    type: simple

logging:
  level:
    org.springframework: DEBUG # ??? ?? ??
    com.rsnotice: DEBUG # ???? ???? ??? ??
